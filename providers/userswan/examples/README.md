# Examples

`userswan.env.example` contains documentation-only addresses. Copy it outside the repository or source it only after replacing every deployment-specific value.

`rdp-local-probe.py` tests an RDP mapping **only through the localhost listener**. It must not be pointed at the protected remote address when validating the full userswan forwarding architecture.
